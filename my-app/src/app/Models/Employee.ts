export interface Employee {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  phoneNumber: string;
  hireDate: string;   // or Date (see note below)
  jobTitle: string;
  salary: number;
}
