import { WaringDirective } from './waring.directive';

describe('WaringDirective', () => {
  it('should create an instance', () => {
    const directive = new WaringDirective();
    expect(directive).toBeTruthy();
  });
});
